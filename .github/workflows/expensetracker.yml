Skip to content
Search or jump to…

Pull requests
Issues
Marketplace
Explore
 
@ZaryabAslam 
ZaryabAslam
/
Expense_Tracker
1
00
 Code
 Issues 0
 Pull requests 0 Actions
 Projects 0
 Wiki
 Security 0
 Insights
 Settings
Expense_Tracker/.github/workflows/main.yml
@ZaryabAslam ZaryabAslam Create main.yml
5e510b6 3 days ago
42 lines (33 sloc)  1.24 KB
  
# This is a basic workflow to help you get started with Actions

name: React app deploy to surge

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
    - uses: actions/checkout@v2

    # Runs a single command using the runners shell
    - name: Install NodeJS
      uses: actions/setup-node@v2-beta
      with:
        node-version: 12
  
    - uses: borales/actions-yarn@v2.0.0
      with:
        cmd: install #will run 'yarn install' command
    
    - name: Build React App
      run: yarn build
      
    - name: Install Surge
      run: npm install --global surge
      
    - name: Deploy to Surge
      run: surge ./build Expensetracker-by-samisahai.surge.sh --token ${{ secrets.SURGE_TOKEN }}
